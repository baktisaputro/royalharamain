# Log & temp
*.log
*.tmp

# file sekali-pakai yang bisa memuat password (jangan pernah commit)
app/setup_password.php

# Gambar asli upload pengguna (besar) - hanya versi ringan (slide_*) yang di-commit
uploads/hero/1*.webp
